import type { Metadata } from "next";
export const metadata: Metadata = { title: "Apply — VARL Careers" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
